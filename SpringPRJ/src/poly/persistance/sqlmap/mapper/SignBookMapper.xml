<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="poly.persistance.mapper.ISignBookMapper">

<select id="getSignBookList" parameterType="SignBookDTO" resultType="SignBookDTO">
SELECT * FROM SIGNBOOK_INFO
</select>

<select id="getSignBookDetail" parameterType="SignBookDTO" resultType="SignBookDTO">
SELECT
	POST_NO,
	POST_TITLE,
	IMG_ONE,
	IMG_TWO,
	SIGN_INFO,
	SIGN_INST
FROM SIGNBOOK_INFO
WHERE POST_NO = #{post_no}
	
</select>

</mapper>